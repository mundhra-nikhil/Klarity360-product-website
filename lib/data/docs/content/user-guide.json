{
  "html": "<p><strong>Klarity 360</strong></p><p>End-to-End Application Flow</p><h1><strong>1. Application Purpose</strong></h1><p>Klarity 360 is a reconciliation and validation platform used to compare datasets between source and target systems — any JDBC-supported database (Oracle, SQL Server, PostgreSQL, MySQL, and others) and Microsoft Fabric. It is built for migration and transformation projects where teams need evidence that data landed in the target completely and accurately.</p><p>Users define validation processes through configurable table/column mappings, apply comparison rules with tolerance thresholds, execute row-level and rule-level validation, and generate reconciliation reports suitable for sign-off and audit. AI-assisted discovery reduces the manual effort of mapping large schemas.</p><h1><strong>2. User Roles</strong></h1><p><strong>QA User</strong> — owns the technical workflow: configures connections, creates processes, defines mappings and rules, executes runs, investigates mismatches, and exports reports. On failure, the QA user determines whether the cause is a genuine data issue, a mapping error, or an over-strict rule.</p><p><strong>Business User</strong> — consumes outcomes: reviews pass/fail results on the dashboard, checks reconciliation reports for business-critical fields, and receives emailed reports for sign-off. Does not create or modify processes.</p><h1><strong>3. High-Level Application Flow</strong></h1><p>The user signs in via Microsoft SSO and lands on the Dashboard. After ensuring the needed source and target connections exist, they create a process through a four-step wizard — connections, mapping, rules, run. The dashboard reflects live execution status, and on completion the user reviews results and downloads or emails the reconciliation report.</p><p><em>Login → Dashboard → Connections → New Process (Steps 1–4) → Execution → Report &amp; Analysis → Recon / Email Report</em></p><h1><strong>4. Login Flow</strong></h1><p>The application offers a single option: <strong>Login with Microsoft</strong>. Clicking it redirects to Microsoft SSO, where organizational credentials are authenticated and access permissions validated — there is no local username/password fallback. On success the user lands on the Dashboard; on failure an error is shown and the user stays on the login page. Expired sessions redirect back to login for re-authentication.</p><h1><strong>5. Dashboard</strong></h1><p>The Dashboard is the operational landing page, showing validation health at a glance.</p><p><strong>KPI cards: </strong>Total Processes, Rows Compared (cumulative records compared, indicating coverage), Total Rules, Rules Passed, and Rules Failed (rules whose latest run failed, signalling discrepancies to investigate).</p><p><strong>Recent Processes table: </strong>one row per execution, with Process Name, Process Code, Status (Running/Passed/Failed), Last_Run, Source–Target, Rules Failed, and Actions.</p><p><strong>Status logic: </strong>a process is <strong>Passed</strong> when all rules pass, <strong>Failed</strong> when at least one critical rule fails. Criticality is set per rule during configuration; non-critical failures are reported but don't fail the run.</p><h1><strong>6. Dashboard Actions</strong></h1><p>Completed processes (Passed or Failed) share these actions: <strong>View</strong> (opens Report &amp; Analysis — <em>not in current release</em>), <strong>Copy</strong> (duplicates the configuration), <strong>Export Config</strong>, <strong>Recon Report</strong>, <strong>Email Report</strong>, and <strong>Delete</strong> (with confirmation).</p><p>A <strong>Passed</strong> process additionally offers <strong>Schedule &amp; Run</strong> (reopens at the execution step). A <strong>Failed</strong> process instead offers <strong>Retry</strong>, which re-runs the identical configuration and updates the dashboard on completion — and even failed runs keep their partial outputs and failure reports available for investigation. A <strong>Running</strong> process offers View, Refresh, Copy, and Export Config; Pause and Stop are planned but not in the current release, so a running execution cannot be interrupted.</p><h1><strong>7. Connections Module</strong></h1><p>Connections are reusable and selected by name inside processes. The application supports <strong>any database with a JDBC driver</strong> (Oracle, SQL Server, PostgreSQL, MySQL, etc.) and <strong>Microsoft Fabric</strong>.</p><p><strong>Create: </strong>the user completes a form with Connection Name, Connection Type, credentials, and server/file details — the fields adapt to the type (a JDBC connection needs host, port, database/service name, and credentials; Fabric needs workspace and lakehouse details with the appropriate auth method).</p><p><strong>Validate: Test Connection</strong> attempts a live connection. Success makes the connection selectable in processes; failure shows a details option with the underlying error so the user can correct and re-test.</p><p><strong>Delete: </strong>blocked when the connection is referenced by any process — <em>“Connection is associated with one or more processes and cannot be deleted.”</em> — preventing orphaned processes that would fail at runtime.</p><h1><strong>8. New Process Creation</strong></h1><p><strong>Step 1 — DB Connection. </strong>Enter Process Name and Description, select Source and Target connections. Mandatory fields and connection accessibility are validated before Step 2, so mapping never starts against an unreachable database.</p><p><strong>Step 2 — Table &amp; Column Mapping. </strong>Two approaches: <strong>Auto Discover with AI</strong> inspects both schemas and suggests table pairs, mapping columns via names, metadata similarity, and structural matching; suggestions are reviewable — accept, adjust, or remove them, with unconfident matches left for manual handling. <strong>Add Mapping Manually</strong> lets the user pick source/target tables and map columns individually — the fallback for renamed or restructured schemas. Mandatory mappings and metadata availability are validated before Step 3.</p><p><strong>Step 3 — Comparison Rules. </strong>Each rule has a name, description, tolerance options, and report options. Three rule types:</p><ul><li><strong>Case Sensitivity</strong> — whether “SMITH” vs “Smith” counts as a mismatch.</li><li><strong>Null Check</strong> — flags values null on one side but populated on the other, catching silent data loss.</li><li><strong>Tolerance Validation</strong> — numeric values may differ within a threshold, absorbing rounding/precision differences.</li></ul><p>Output scope per rule: Missing records only, Value mismatches only, or All differences.</p><p><strong>Step 4 — Run Process. </strong>Only <strong>Manual Run</strong> is supported currently. <strong>Run Now</strong> saves the configuration, creates an execution instance with a unique Process Code, and starts the validation engine.</p><h1><strong>9. Process Execution</strong></h1><p>An execution moves from <strong>Running</strong> to <strong>Passed</strong> or <strong>Failed</strong>, with the dashboard updating dynamically. The engine runs four sequential stages:</p><ol><li><strong>Schema Validation</strong> — column presence, data types, structural compatibility; broken mappings fail fast before data is read.</li><li><strong>Data Validation</strong> — row-level comparison using the compare key columns, identifying missing records, duplicates, and value mismatches.</li><li><strong>Rule Validation</strong> — applies the configured business rules, recording pass/fail per rule.</li><li><strong>Tolerance Validation</strong> — reclassifies numeric differences within threshold as matches.</li></ol><p>If a stage hits an unrecoverable error (e.g., a connection dropping mid-run), the execution is marked Failed and outputs generated so far are preserved.</p><h1><strong>10. Report Generation &amp; Analysis</strong></h1><p>After execution, the system generates the recon report, per-rule analysis, mismatch output, and summary metrics.</p><p><strong>Report &amp; Analysis page</strong><em> (not fully implemented)</em> — will present rule outcomes, comparison statistics, and failed records in-app.</p><p><strong>Recon Report</strong> downloads an Excel workbook with these sheets:</p><p><strong>Summary</strong> — Process Code, Rule Name, Status (Match/Mismatch), Report Run Date, Rule Description, Database Type, Table Name, Field Count, Records Count, Matched Records, Mismatch Records, Match Accuracy (%), Value Differences, Top 5 Columns Cover (%), Missing Records, and Duplicate Records.</p><p><strong>Differences</strong> — row-level mismatch detail: Compare Key Columns, Key Values, Mismatch Columns, Source and Target Values side by side, Mismatch Repeat Count, and Weightage % (contribution to the overall result). Repeated high-weightage patterns typically indicate systematic transformation issues rather than isolated errors.</p><p><strong>Missing (Source) / Missing (Target)</strong> — records present in one dataset but absent from the other.</p><p><strong>Duplicate (Source) / Duplicate (Target)</strong> — duplicates in each dataset; both sheets list Compare Key Columns, the duplicated Key Values, and the Repeated Count.</p><p><strong>Email Report</strong> opens a popup for recipient addresses and a message body (subject line is pre-set) and sends the workbook as an attachment — the standard delivery path for Business users.</p><h1><strong>11. Notifications &amp; Error Handling</strong></h1><p>Email notifications are sent on process completion, process failure, and report generation.</p><p><strong>Connection failure</strong> — an error is shown and retry is allowed; a mid-execution drop marks the run Failed, after which the user can fix the connection and Retry from the dashboard. <strong>Rule failure</strong> — a critical rule failing marks the process Failed; this is an expected validation outcome, and failure reports remain available for investigation. <strong>Report failure</strong> — the user is notified so generation can be re-triggered rather than silently missing output.</p><h1><strong>12. End-to-End Use Case</strong></h1><p>A QA user validates a CUSTOMERS table migrated from Oracle to a Fabric Lakehouse. They create and test both connections, then build a process: Auto Discover maps the table and columns, a Null Check is set on customer ID and email, and a Tolerance rule absorbs rounding on account balance. The run fails — emails populated in Oracle are null in Fabric. The Differences sheet pinpoints the affected keys; after the migration team fixes the load, Retry passes and the report is emailed to the Business user for sign-off.</p><h1><strong>13. Summary</strong></h1><p>Klarity 360 covers the full migration-validation lifecycle — connection management, AI-assisted mapping, rule-based comparison, execution monitoring, and audit-ready reconciliation reporting — giving QA users row- and rule-level diagnostics and Business users clear, shareable pass/fail outcomes.</p>"
}